{
  "name": "io.cordova.hellocordova",
  "displayName": "HelloCordova",
  "version": "1.0.0",
  "description": "Small app meant to reproduce the errors when creating a worker in Android Webview 130.0.6710.0/130.0.6723.58.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cordova run browser",
    "build": "cordova platform rm android && cordova platform add  android && cordova run android"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Apache Cordova Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "cordova-android": "^9.1.0",
    "cordova-browser": "^6.0.0",
    "cordova-plugin-whitelist": "^1.3.5"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-whitelist": {}
    },
    "platforms": [
      "browser",
      "android"
    ]
  }
}